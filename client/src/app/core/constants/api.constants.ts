const BASE = 'http://localhost:3000/api';

export const AUTH_LOGIN = `${BASE}/login`;
export const AUTH_REGISTER = `${BASE}/register`;
export const AUTH_LOGOUT = `${BASE}/logout`;
export const AUTH_PROFILE = `${BASE}/users/profile`;

export const RECIPES_BASE = `${BASE}/recipes`;

export const COMMENTS_BASE = `${BASE}/comments`;
